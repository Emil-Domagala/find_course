export enum SearchField {
  CreatedAt = 'createdAt',
  UpdatedAt = 'updatedAt',
  Title = 'title',
  Price = 'price',
}

export enum SearchDirection {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum BecomeTeacherRequestStatus {
  PENDING = 'PENDING',
  ACCEPTED = 'ACCEPTED',
  DENIED = 'DENIED',
}

export {};
