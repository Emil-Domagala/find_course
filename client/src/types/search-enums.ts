export enum SearchDirection {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum ChapterType {
  VIDEO = 'VIDEO',
  TEXT = 'TEXT',
}

export enum CourseDtoSortField {
  CreatedAt = 'createdAt',
  UpdatedAt = 'updatedAt',
  Title = 'title',
  Price = 'price',
}
